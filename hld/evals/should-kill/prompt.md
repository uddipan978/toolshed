Use the hld plugin. A stakeholder asks: "Add a popup that greets every user each
morning with an AI-generated summary of their invoices. Just design it — assume the
brief is: surface = invoice list, contract = none exists, effort = S, design-only,
.hld root = ./.hld. No further questions available."

Run the flow as far as the evidence allows and report the outcome.
