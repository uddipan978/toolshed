# UX report — invoice AI summary popup
Kill-door verdict: KILL — fails B=MAP (no user prompt exists for a morning popup; motivation
unverified), dumps Tesler complexity on the user (dismissal becomes their daily job), and the
projected engagement lift is below the one-sixth effect-size floor.

Evidence: symptoms.csv S41 (interruption fatigue, replicated); principles.csv P17.
