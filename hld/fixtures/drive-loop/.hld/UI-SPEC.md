# UI spec
Region chip-cell → Order.status (IA object model).
## Rejected alternatives
Colored row backgrounds — fails contrast at desk density; icon-only chips — fails the
copy inventory rule (labels required).
